Real R1-exported runtime snapshots belong in this directory.

Use the log drawer header quadruple-tap or the `export snapshot` voice command to dump a live snapshot,
then copy the JSON here for harness-backed regression checks.
